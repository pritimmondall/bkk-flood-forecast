# Bangkok Flood Forecast — Vercel Serving Dependencies
#
# This is intentionally stripped to what the FastAPI service imports at
# runtime. Heavy training/analysis deps (torch, rasterio, geopandas,
# scikit-image, scipy, jupyterlab, …) are in requirements-dev.txt.
#
# Vercel's Lambda limit is 250 MB unzipped. The packages below are ~160 MB,
# leaving headroom for the model artifacts bundled via includeFiles.

# --- Core serving ---------------------------------------------------------
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6

# --- Data layer (duckdb streams parquet within a fixed memory cap) ---------
duckdb>=1.0
pandas>=2.0
pyarrow>=15.0          # Parquet
numpy>=1.26
PyYAML>=6.0            # config/config.yaml

# --- Model inference ------------------------------------------------------
lightgbm>=4.3
scikit-learn>=1.4      # used in calibration / metrics helpers

# --- Live data collectors -------------------------------------------------
httpx>=0.27
requests>=2.31

# --- CAP alert generation -------------------------------------------------
lxml>=5.1
jsonschema>=4.21
